// var controllerOptions = {};

// var controller = Leap.Controller();

// Leap.loop(controllerOptions, function(frame) {
//     if (frame.hands.length > 0) { //If there is at least one hand over the device

//     }
// })